using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;

public class Teleport : MonoBehaviour
{
    // Start is called before the first frame update
    //partial trigger pull = show cylinder where player would go [squeeze > 0 = holding, grabpinch true = pressing]
    // full trigger pull = teleport
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
